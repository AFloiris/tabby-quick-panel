import { ChangeDetectorRef, ElementRef, OnDestroy, OnInit } from '@angular/core';
import { ConfigService } from 'tabby-core';
import { QuickPanelCommand } from '../api';
import { QuickPanelCommandSenderService } from '../services/commandSender.service';
import { QuickPanelCommandStoreService } from '../services/commandStore.service';
import { QuickPanelSidebarHostService } from '../services/sidebarHost.service';
interface SidebarGroup {
    id?: string;
    key: string;
    name: string;
    commands: QuickPanelCommand[];
}
export declare class QuickPanelSidebarComponent implements OnInit, OnDestroy {
    private config;
    private sender;
    private store;
    private sidebarHost;
    private changeDetector;
    panelRoot: ElementRef<HTMLElement>;
    searchInput: ElementRef<HTMLInputElement>;
    filter: string;
    groups: SidebarGroup[];
    private collapsedGroupKeys;
    private panelIsResizing;
    private panelStartWidth;
    private panelStartX;
    private selectedKey?;
    private subscription;
    constructor(config: ConfigService, sender: QuickPanelCommandSenderService, store: QuickPanelCommandStoreService, sidebarHost: QuickPanelSidebarHostService, changeDetector: ChangeDetectorRef);
    ngOnInit(): void;
    ngOnDestroy(): void;
    focusPanel(): void;
    sendAndReturnFocus(command: QuickPanelCommand): void;
    onKeyDown(event: KeyboardEvent): void;
    startResize(event: MouseEvent): void;
    onMouseMove(event: MouseEvent): void;
    stopResize(): void;
    refresh(): void;
    trackByGroup(_index: number, group: SidebarGroup): string;
    trackByCommand(_index: number, command: QuickPanelCommand): string;
    private createSidebarGroup;
    commandsForGroup(group: SidebarGroup): QuickPanelCommand[];
    toggleGroup(group: SidebarGroup): void;
    isCollapsed(group: SidebarGroup): boolean;
    isGroupSelected(group: SidebarGroup): boolean;
    isCommandSelected(command: QuickPanelCommand): boolean;
    ensureSelection(): void;
    private moveSelection;
    private enterGroup;
    private exitGroup;
    private confirmSelection;
    private getSelectedItem;
    private getVisibleItems;
    private scrollSelectedIntoView;
    private isNavigationKey;
    private isSearchKey;
    private applySearchKey;
    private groupKey;
    private commandKey;
}
export {};
